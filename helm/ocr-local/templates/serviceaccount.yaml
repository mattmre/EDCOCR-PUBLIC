{{- if .Values.rbac.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ocr-local.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ocr-local.labels" . | nindent 4 }}
{{- end }}
