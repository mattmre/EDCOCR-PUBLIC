[pytest]
DJANGO_SETTINGS_MODULE = coordinator.settings_test
pythonpath = .
markers =
    integration: marks tests as integration tests requiring external services (deselect with '-m "not integration"')
filterwarnings =
    error:The NumPy module was reloaded.*:UserWarning
