# Coordinator (Django + Celery) Python dependencies
#
# Pillow and PyMuPDF versions must match the root requirements.txt.
# Root requirements.txt is authoritative for shared image/PDF pins.
# Run scripts/check_deps_sync.py to verify sync.

Django>=5.2.14,<6.0
psycopg2-binary==2.9.12
celery[rabbitmq]==5.6.3
redis>=7.0.1,<8.0.0
django-celery-results==2.6.0
django-celery-beat==2.9.0
flower==2.0.1
gunicorn==26.0.0
boto3>=1.43.10,<2.0.0

# Must match root requirements.txt (authoritative source).
Pillow==12.2.0

prometheus-client>=0.25.0,<1.0.0

# Must match root requirements.txt (authoritative source).
PyMuPDF==1.27.2.2

PyYAML==6.0.3
cryptography>=48.0.0
django-otp>=1.5
qrcode>=7.4

# , item C7 -- DNS-SD federation cluster auto-discovery.
# Both deps are *optional* at import time; the discovery module imports
# cleanly without them and surfaces a clear error only when used.
dnspython>=2.0,<3.0
kubernetes>=28.0,<32.0
