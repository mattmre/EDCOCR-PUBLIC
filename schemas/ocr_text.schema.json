{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ocr-local.dev/schemas/ocr_text.schema.json",
  "title": "OCR Text Output",
  "description": "Plain text extracted from a document by the OCR pipeline. Written to EXPORT/TEXT/<subfolder>/<document_name>.txt as a raw UTF-8 text file.",
  "type": "object",
  "properties": {
    "artifact_type": {
      "type": "string",
      "const": "ocr_text",
      "description": "Fixed identifier for this output type."
    },
    "file_extension": {
      "type": "string",
      "const": ".txt",
      "description": "File extension for the text artifact."
    },
    "encoding": {
      "type": "string",
      "const": "utf-8",
      "description": "Character encoding of the text file."
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of the artifact."
    }
  },
  "required": ["artifact_type", "file_extension", "encoding"]
}
